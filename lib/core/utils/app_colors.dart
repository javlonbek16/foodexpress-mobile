import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF5B5FEF);
  static const primaryDark = Color(0xFF4548D4);
  static const secondary = Color(0xFFF5A623);

  // Background
  static const scaffold = Color(0xFFF8F9FC);
  static const surface = Color(0xFFF2F4F7);
  static const card = Colors.white;

  // Text
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFF9CA3AF);
  static const textWhite = Colors.white;

  // Border
  static const border = Color(0xFFE5E7EB);

  // Status
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  // Others
  static const disabled = Color(0xFFB8C1CC);

  // Restaurant
  static const restaurantOpen = Color(0xFFE9F9EF);
  static const restaurantOpenText = Color(0xFF16A34A);

  static const restaurantClosed = Color(0xFFFEECEC);
  static const restaurantClosedText = Color(0xFFDC2626);

  // Category
  static const categorySelected = primary;
  static const categorySelectedText = Colors.white;

  static const categoryUnselected = Colors.white;
  static const categoryUnselectedText = textSecondary;

  // FAB
  static const fab = primary;

  // Progress
  static const progress = primary;

  // Modal
  static const modalBackground = Colors.white;

  // Shadow
  static const shadow = Color(0x14000000);
}
